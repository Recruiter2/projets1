// someElement.innerText = `${yourText.substring(0, someLength-3)}...`;

var monInfo = document.getElementById("info_text");


document.getElementById("info_text").innerText = `${monInfo.substring(0, 10)}...`;